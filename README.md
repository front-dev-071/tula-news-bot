# Tula News Agent

Новостной агент для сбора и анализа новостей по Тульской области из открытых источников информации.
Проект написан на Python с использованием современных практик разработки.

## 🚀 Возможности

- **Сбор новостей** из различных источников (Google News)
- **Анализ релевантности** новостей
- **Кэширование** результатов для оптимизации производительности
- **Красивый CLI интерфейс** с таблицами и прогресс-барами
- **Экспорт** новостей в различные форматы (JSON, CSV, TXT)
- **Поиск** и фильтрация новостей
- **Статистика** и аналитика

## 📋 Требования

- Python 3.8+
- Зависимости из `requirements.txt`

## Ограничения

- Ищем информацию в рамках города Тулы и Тульской области
- Информацию получаем только из открытых источников
- Проект запускается локально

## 🛠️ Установка и настройка

1. **Клонирование репозитория:**
   ```bash
   git clone <repository-url>
   cd tula-news-bot
   ```

2. **Создание виртуального окружения:**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   # или
   venv\Scripts\activate  # Windows
   ```

3. **Установка зависимостей:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройка окружения:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env при необходимости
   ```

## 🏗️ Архитектура проекта

```
tula-news-bot/
├── main.py                 # Точка входа в приложение
├── src/                    # Основной исходный код
│   ├── cli/                # CLI интерфейс (Typer + Rich)
│   │   └── commands.py     # Команды CLI
│   ├── core/               # Ядро приложения
│   │   └── config.py       # Конфигурация и .env
│   ├── news/               # Работа с новостями
│   │   ├── models.py       # Модели данных (NewsArticle)
│   │   ├── sources.py      # Источники новостей (Google News)
│   │   ├── collector.py    # Сборщик новостей
│   │   └── cache.py        # Кэширование
│   └── services/           # Бизнес-логика
│       └── news_service.py # Основной сервис новостей
├── tests/                  # Тесты (pytest)
├── storage/                # Хранилище данных
├── logs/                   # Логи приложения
└── requirements.txt        # Зависимости
```

## 🎮 Использование

### Основные команды

```bash
# Запуск приложения
python main.py [команда] [опции]

# Сбор новостей
python main.py collect --query "Тула" --limit 20

# Показать последние новости
python main.py show --limit 15 --sort date

# Поиск новостей
python main.py search "происшествие" --limit 10 --min-relevance 0.5

# Показать новости с кликабельными ссылками
python main.py links --limit 10

# Статистика
python main.py stats

# Экспорт новостей
python main.py export --format json --limit 50

# Очистка кэша
python main.py clear-cache

# Показать конфигурацию
python main.py config-show
```

### Описание команд

- **`collect`** - Сбор свежих новостей из источников
- **`show`** - Показ сохраненных новостей с сортировкой
- **`search`** - Поиск новостей по ключевым словам
- **`links`** - Показ новостей с кликабельными ссылками
- **`stats`** - Статистика по новостям и источникам
- **`export`** - Экспорт в файл (JSON/CSV/TXT)
- **`clear-cache`** - Очистка кэша
- **`config-show`** - Текущая конфигурация

## 🔧 Конфигурация

Основные настройки в файле `.env`:

```env
# Приложение
APP_NAME=TulaNewsAgent
LOG_LEVEL=INFO

# Новости
DEFAULT_REGION=Тула, Тульская область
NEWS_LIMIT=10
LANGUAGE=ru
REQUEST_TIMEOUT=30

# Хранилище
STORAGE_PATH=storage/news_data
```

## 📊 Модели данных

### NewsArticle
Основная модель новости:
- `id` - Уникальный идентификатор
- `title` - Заголовок
- `url` - Ссылка на новость
- `source` - Источник
- `published_at` - Дата публикации
- `summary` - Краткое содержание
- `relevance_score` - Оценка релевантности (0-1)
- `region` - Регион
- `category` - Категория новости

## 🧪 Тестирование

```bash
# Запуск всех тестов
pytest

# Запуск с покрытием
pytest --cov=src

# Запуск конкретных тестов
pytest tests/test_service.py
pytest -m unit  # Только unit тесты
pytest -m integration  # Только интеграционные
```

## 📝 Логирование

Логи сохраняются в директории `logs/`:
- Формат: `tula_news_YYYY-MM-DD.log`
- Ротация: ежедневно
- Хранение: 7 дней
- Уровни: DEBUG, INFO, WARNING, ERROR

## 🔄 Кэширование

Система кэширования оптимизирует запросы:
- TTL: 1 час (настраивается)
- Хранение: `storage/cache/`
- Автоматическая очистка устаревших данных

## 🛡️ Обработка ошибок

- **Retry механизм** - до 3 попыток с экспоненциальным backoff
- **Обработка сетевых ошибок** - таймауты, проблемы с подключением
- **Валидация данных** - проверка URL, заголовков, релевантности
- **Graceful degradation** - продолжение работы при частичных сбоях

## 📈 Релевантность новостей

Алгоритм оценки релевантности для Тульской области:
- Ключевые слова: "тула", "туле", "тульск", "област"
- Точное совпадение региона
- Оценка от 0 до 1

## 🚀 Разработка

### Добавление нового источника новостей

1. Создать класс в `src/news/sources.py`:
```python
class CustomNewsSource(NewsSource):
    def fetch_news(self, query: Optional[str] = None, limit: int = 10) -> List[NewsArticle]:
        # Реализация сбора новостей
        pass
```

2. Добавить в `NewsSourceFactory`

### Структура CLI команд

Команды определены в `src/cli/commands.py` с использованием:
- **Typer** - для CLI интерфейса
- **Rich** - для красивого вывода (таблицы, прогресс-бары, панели)

## 📦 Зависимости

Основные зависимости:
- `typer` - CLI интерфейс
- `rich` - Красивый терминальный вывод
- `pygooglenews` - Google News API
- `beautifulsoup4` - Парсинг HTML
- `newspaper3k` - Извлечение контента
- `loguru` - Логирование
- `python-dotenv` - Управление окружением
- `pytest` - Тестирование

## 🤝 Вклад в проект

1. Fork репозитория
2. Создать feature branch
3. Внести изменения
4. Добавить тесты
5. Запустить тесты
6. Создать Pull Request

## 📄 Лицензия

[Добавить информацию о лицензии]

---

**Проект разработан как учебный пример новостного агента с фокусом на Тульскую область.**